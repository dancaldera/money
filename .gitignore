# Virtual environment
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Secrets — never commit API keys
.env
.env.*
!.env.example

# Project data & outputs (regenerated, not source).
# Anchored to the repo root so they don't also match src/trading/data/ (source).
/data/
/results/

# OS / editor
.DS_Store
.idea/
.vscode/
